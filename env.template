# ConceptHub Environment Variables Template
# Copy this file to .env.local (for local development) or .env (for production)
# Do not commit .env files to version control

# API Server URL
# This is the base URL for the ConceptHub backend API server
# For client-side (browser) access, use NEXT_PUBLIC_SERVER_URL
# For server-side (Node.js) access, use SERVER_URL
# Default: http://localhost:8000

# Client-side API URL (exposed to browser)
# Must be prefixed with NEXT_PUBLIC_ to be accessible in the browser
NEXT_PUBLIC_SERVER_URL=http://localhost:8000

# Server-side API URL (Node.js only)
# Used for server-side rendering and API routes
# If not set, falls back to NEXT_PUBLIC_SERVER_URL
SERVER_URL=http://localhost:8000


